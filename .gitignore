# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments
.venv/
venv/
.env
.env.*
!.env.example

# Tooling caches
.ruff_cache/
.pytest_cache/
.mypy_cache/
.langgraph_api/

# Node / web apps (apps/web also ships its own .gitignore)
node_modules/
.next/

# OS / editor
.DS_Store
.idea/
.vscode/

# NOTE: apps/nora/src/nora/data/curry_nomad.db is intentionally committed (the bundled,
# deterministic demo dataset). Do not add a blanket *.db / *.sqlite ignore.

# Operations runtime DB (writable; rebuilt by `python -m nora.operations.seed`). A specific
# path, NOT a blanket *.db glob, so the committed dataset above stays tracked.
apps/nora/src/nora/data/runtime/
