"""The bundled dataset: a deterministic Sri Lankan spice business in SQLite.

`seed.py` builds `curry_nomad.db` (committed) from a fixed RNG seed and a fixed date window,
so it reproduces byte-for-byte and the analytics evals are stable.
"""
