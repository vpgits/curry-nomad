"""Marketing Studio — the WORKFLOW.

A deterministic, multi-step pipeline that turns a product into an image **Instagram post** (a
structured brief + a hero image and per-shot stills): chaining (ideate → write copy → storyboard →
assemble → render), parallelization (concept ideation + per-shot image prompts), an evaluator-
optimizer loop (critique → revise), and human-review interrupts (concept, copy, and the generated
stills). Predictable shape → a workflow, not an open-ended agent.
"""
