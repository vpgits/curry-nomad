"""Marketing Studio — the WORKFLOW.

A deterministic, multi-step pipeline that turns a product into a structured video-ad brief:
chaining (ideate → script → storyboard → assemble), parallelization (concept ideation +
per-shot prompts), an evaluator-optimizer loop (critique → revise), and — added in M3 — a
human-review interrupt. Predictable shape → a workflow, not an open-ended agent.
"""
