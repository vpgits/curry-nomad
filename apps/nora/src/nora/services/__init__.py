"""External effects behind interfaces (ports & adapters).

`interfaces` defines the Protocols; `spice_db` and `renderer` are the bundled, deterministic
implementations used by default. Real adapters (OpenRouter renderer) are optional swaps.
"""
